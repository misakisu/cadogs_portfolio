<div class="container">
<h4><strong>プロフィール</strong></h4>
<div class="row profile">
  <div class="col-xs-12 col-sm-4">
    <div class="profile-name"><%= @user.family_name %><%= @user.given_name %></div>
    <div><%= @user.family_name_kana %> <%= @user.given_name_kana %></div>
    <div><i class="fas fa-phone"></i> <%= @user.phone_number %></div>
    <div><i class="fas fa-envelope"></i> </th><td><%= @user.email %></div>
  　<%= link_to edit_user_registration_path do %>
    <button class="btn btn-success">登録情報編集</button>
  　<% end %>
  </div>
</div>

<h4><strong>予約一覧</strong></h4>
  <% @reservations.each do |reservation| %>
    <div class="reservation-box">
      <%= render partial: 'reservations/all', locals: { reservation: reservation} %>
    </div>
  <% end %>
</div>



